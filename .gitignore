# Claude instruction/memory (local-only dev guidance)
.claude/
CLAUDE.md

# Clang-Format build
.clangd

# CMake build (standalone component builds / consuming examples)
build/
.cache/
managed_components/

# MacOS
.DS_Store
