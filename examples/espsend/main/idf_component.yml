dependencies:
  # Both sendmy layers are consumed from this repo by relative path, so the
  # example builds against the in-tree component sources (../../../<component>).
  sendmy_link:
    path: ../../../sendmy_link
  sendmy_carrier:
    path: ../../../sendmy_carrier
